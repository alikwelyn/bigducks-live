{
  "name": "bigducks-golive-sender",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Experimental Go Live sender. Pushes a real Go Live stream (STREAM_CREATE + DAVE/E2EE + RTP) into a voice channel so NATIVE Discord clients can watch it. Does not consult the Apex experiment gate, so it ignores the Brazilian region block. Test tool only.",
  "engines": {
    "node": ">=22.4.0"
  },
  "dependencies": {
    "@dank074/discord-video-stream": "^7.0.0",
    "@lng2004/discord.js-selfbot-v13": "^3.7.2",
    "ffmpeg-static": "^5.2.0"
  }
}
