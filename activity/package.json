{
  "name": "bigducks-discord-stream-activity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "vite build",
    "smoke": "node scripts/smoke.mjs",
    "start": "node relay/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "vite": "^6.0.11",
    "@vitejs/plugin-basic-ssl": "^2.0.0"
  }
}
